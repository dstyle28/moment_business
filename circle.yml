machine:
  timezone:
    America/Los_Angeles # Set the timezone
  node:
    version: 0.10.32

test:
  pre:
    - npm install -g karma-cli
    - npm install -g bower
    - npm install

  override:
    - karma start ./moment_business/karma.conf.js --single-run

