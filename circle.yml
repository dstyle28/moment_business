machine:
  timezone:
    America/Los_Angeles # Set the timezone
  node:
    version: 0.10.32

test:
  pre:
    - npm install -g karma
    - npm install -g bower
    - npm install

  override:
    - bundle install:
        timeout: 300
    - karma start ./karma.conf.js --single-run

